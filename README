Test project for CoffeeScript & Backbone with AMD and Jasmine


Install Redis and start the server on localhost using the default port.

Running on http://coffeebone.herokuapp.com/

Use Play! Framework 1.2.4

Usage:
./redis-server &
play run

Go to localhost:9000

Jasmine runner: localhost:9000/jasmine

Technologies used:
- Play! Framework
- Redis + RedisToGo on Heroku
- Twitter Bootstrap
- Compass Framework + SCSS
- Backbone.js
- Require.js with optimizer
- CoffeeScript
- Jasmine

In order to run the optimizer script, install Node.
